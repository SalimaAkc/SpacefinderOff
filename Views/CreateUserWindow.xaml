<Window x:Class="SpacefinderOff.Views.CreateUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create New User" Height="450" Width="400"
        Background="#E8F0FE">

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Create New User" FontSize="24" FontWeight="Bold" Margin="0,0,0,25" Foreground="#1E3A8A" />
            

            <TextBlock Text="Full Name" Margin="0,0,0,5" Foreground="#1E40AF" FontWeight="SemiBold"/>
            <TextBox Name="FullNameTextBox" Margin="0,0,0,15" Padding="5" BorderBrush="#3B82F6" BorderThickness="1" />

            <TextBlock Text="Email" Margin="0,0,0,5" Foreground="#1E40AF" FontWeight="SemiBold"/>
            <TextBox Name="EmailTextBox" Margin="0,0,0,15" Padding="5" BorderBrush="#3B82F6" BorderThickness="1" />

            <TextBlock Text="Phone" Margin="0,0,0,5" Foreground="#1E40AF" FontWeight="SemiBold"/>
            <TextBox Name="PhoneTextBox" Margin="0,0,0,15" Padding="5" BorderBrush="#3B82F6" BorderThickness="1" />

            <TextBlock Text="Role" Margin="0,0,0,5" Foreground="#1E40AF" FontWeight="SemiBold"/>
            <ComboBox Name="RoleComboBox" Margin="0,0,0,15" Padding="5" BorderBrush="#3B82F6" BorderThickness="1">
                <ComboBoxItem Content="User"/>
                <ComboBoxItem Content="Admin"/>
            </ComboBox>

            <TextBlock Text="Password" Margin="0,0,0,5" Foreground="#1E40AF" FontWeight="SemiBold"/>
            <PasswordBox Name="PasswordBox" Margin="0,0,0,25" Padding="5" BorderBrush="#3B82F6" BorderThickness="1"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" >
                <Button Content="Cancel" Width="80" Margin="0,0,10,0" Click="CancelButton_Click"
                        Background="#93C5FD" Foreground="White" BorderBrush="#3B82F6" />
                <Button Content="Create" Width="80" Click="CreateButton_Click"
                        Background="#2563EB" Foreground="White" BorderBrush="#1D4ED8" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
